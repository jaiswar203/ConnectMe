import { Layout } from "../src/components";


const Logins = () => {
    
    return (
        <Layout title={`${true ? "Login" : "Sign Up"}`}>
            {/* <Login /> */}
        </Layout>
    );
};

export default Logins;
